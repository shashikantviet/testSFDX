public class CreatNewContact {
    
    public void newContactInsert(){
        String CurrentClassName = String.valueOf(this).substring(0,String.valueOf(this).indexOf(':'));
        System.debug('>>>>>>>>>'+CurrentClassName);
    }
}